{
  "version": "0.0.1",
  "configurations": [
    {
      "//": "Runs the dev server in the pinned lesson3-deps container. `scripts/dev-server.sh` explains why the host node cannot since #214; AGENTS.md → Local stack covers seeding and the public-library switches. Kept to a pointer deliberately — the comment pair this replaced had drifted into being wrong, which is what a rationale kept in a file nobody greps does.",
      "name": "lesson3-dev",
      "runtimeExecutable": "scripts/dev-server.sh",
      "runtimeArgs": [],
      "port": 3000
    }
  ]
}
